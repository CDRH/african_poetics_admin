<p>
  <strong class="text-danger">Remember:</strong>
  Do not add people or roles below until the information above has been saved

  <br>
  <button class="btn btn-primary"
    data-disable-with="Save and edit"
    name="_add_edit"
    type="submit">
    <i class="icon-white icon-ok"></i>
    Save progress before adding person
  </button>
</p>
<hr>
<p>
  Open a new tab/window to add new People not already in this database before adding roles below
  <br>
  <% if Rails.application.config.relative_url_root.present? %>
    <a href="<%= Rails.application.config.relative_url_root %>/person/new/"
  <% else %>
    <a href="../../person/new/"
  <% end %>
    class="btn btn-info" target="_blank">
    <i class="icon-plus icon-white"></i>
    Add a new Person
  </a>
</p>
